name: cool_animations
description: "멋진 애니메이션을 손쉽게 적용할 수 있는 패키지"
version: 0.0.1
homepage: https://github.com/yundal8755/cool_animations

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
