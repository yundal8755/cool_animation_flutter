name: cool_animation_flutter
description: "A Flutter package for easy and concise animations. Supports direction, delay, sequential animations, and visibility-based triggers."
version: 0.0.3
repository: https://github.com/yundal8755/cool_animation_flutter
issue_tracker: https://github.com/yundal8755/cool_animation_flutter/issues

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
